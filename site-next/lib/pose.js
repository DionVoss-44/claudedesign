/* The brain's puppet strings, shared between the WebGL layer (reads it
   every frame) and the scroll choreography (tweens it with GSAP). */
export const pose = { x: 0.72, y: 0.5, s: 1, a: 1, r: 0 };
